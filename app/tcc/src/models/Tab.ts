export class Tab{
    public title:String;
    public root:any;
    public icon:String;

    constructor(title, root, icon){
        this.title=title;
        this.root=root;
        this.icon=icon;
    }
}